== README

This is a fork created to test Heroku's github integration (it needs repo admin access)

On Heroku:
Add a new app. Click into app's 'deploy'. add a pipeline, specify git repository.
On the app.json page, specify postdeploy as

'bundle exec rake db:setup'
